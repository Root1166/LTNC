
#include <stdio.h>
#include <conio.h>
main()
{
    float x,y,z,max3s();
    int s;
    printf("nhap so thu nhat:");
    scanf("%f",&x);
    printf("nhap so thu hai:");
    scanf("%f",&y);
    printf("nhap so thu ba:");
    scanf("%f",&z);
    printf("so lon nhat la=%8.0f",max3s(x,y,z));
    getch();
}
void

{

    float max;
    max = a > b? a: b;
    return(max>c?max:c);
}
